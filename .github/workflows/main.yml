name: Build CarPlay Pro IPA
on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create App Files
        run: |
          # 1. ملف المحرك الأساسي
          cat <<EOF > App.swift
          import SwiftUI
          @main
          struct MyCarApp: App {
              var body: some Scene {
                  WindowGroup { ContentView() }
              }
          }
          EOF

          # 2. ملف تعريف CarPlay (يمنع الكراش ويطلب الظهور في السيارة)
          cat <<EOF > CarPlayScene.swift
          import UIKit
          import CarPlay

          class CarPlaySceneDelegate: UIResponder, CPTemplateApplicationSceneDelegate {
              func templateApplicationScene(_ templateApplicationScene: CPTemplateApplicationScene, didConnect interfaceController: CPInterfaceController) {
                  // هنا يتم الربط مع شاشة السيارة مستقبلاً
              }
          }
          EOF

      - name: Compile Pro Build
        run: |
          # تجميع كل الملفات في ملف تنفيذي واحد
          xcrun -sdk iphoneos swiftc App.swift ContentView.swift CarPlayScene.swift \
          -o MyCarApp \
          -target arm64-apple-ios15.0 \
          -sdk $(xcrun --sdk iphoneos --show-sdk-path) \
          -emit-executable

      - name: Structure IPA
        run: |
          mkdir -p Payload/MyCarApp.app
          mv MyCarApp Payload/MyCarApp.app/
          
          # 3. ملف Info.plist الخارق (يخدع النظام ليظهره في الكار بلاي)
          cat <<EOF > Payload/MyCarApp.app/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleExecutable</key> <string>MyCarApp</string>
              <key>CFBundleIdentifier</key> <string>com.mym.dashboard.pro</string>
              <key>CFBundleName</key> <string>MyDash</string>
              <key>CFBundlePackageType</key> <string>APPL</string>
              <key>CFBundleShortVersionString</key> <string>1.0</string>
              <key>CFBundleVersion</key> <string>1</string>
              <key>LSRequiresIPhoneOS</key> <true/>
              <key>MinimumOSVersion</key> <string>15.0</string>
              <key>UIApplicationSceneManifest</key>
              <dict>
                  <key>UISceneConfigurations</key>
                  <dict>
                      <key>CPTemplateApplicationSceneSessionRoleApplication</key>
                      <array>
                          <dict>
                              <key>UISceneDelegateClassName</key>
                              <string>MyCarApp.CarPlaySceneDelegate</string>
                          </dict>
                      </array>
                  </dict>
              </dict>
          </dict>
          </plist>
          EOF

          zip -r MyCarApp.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: CarPlay-Pro-Final
          path: MyCarApp.ipa
